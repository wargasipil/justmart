// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: connector_iface/v1/connector.proto

package connectorifacev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/justmart/backend/gen/connector_iface/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ConnectorServiceName is the fully-qualified name of the ConnectorService service.
	ConnectorServiceName = "connector_iface.v1.ConnectorService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ConnectorServiceConnectProcedure is the fully-qualified name of the ConnectorService's Connect
	// RPC.
	ConnectorServiceConnectProcedure = "/connector_iface.v1.ConnectorService/Connect"
	// ConnectorServiceListConnectorsProcedure is the fully-qualified name of the ConnectorService's
	// ListConnectors RPC.
	ConnectorServiceListConnectorsProcedure = "/connector_iface.v1.ConnectorService/ListConnectors"
)

// ConnectorServiceClient is a client for the connector_iface.v1.ConnectorService service.
type ConnectorServiceClient interface {
	// Connect is the long-lived outbound stream from a connector to the server.
	// It is public + UNAUTHENTICATED by design: a connector connects freely (no
	// token), suitable for a trusted single-shop LAN. The unary auth interceptor
	// does not cover streams anyway.
	Connect(context.Context, *connect.Request[v1.ConnectRequest]) (*connect.ServerStreamForClient[v1.ServerEvent], error)
	// ListConnectors returns the connectors currently connected + their printers,
	// for the Settings ▸ Printing picker AND the POS receipt printer picker. Open
	// to every POS role (returns only device + printer names, nothing sensitive).
	ListConnectors(context.Context, *connect.Request[v1.ListConnectorsRequest]) (*connect.Response[v1.ListConnectorsResponse], error)
}

// NewConnectorServiceClient constructs a client for the connector_iface.v1.ConnectorService
// service. By default, it uses the Connect protocol with the binary Protobuf Codec, asks for
// gzipped responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply
// the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewConnectorServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ConnectorServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	connectorServiceMethods := v1.File_connector_iface_v1_connector_proto.Services().ByName("ConnectorService").Methods()
	return &connectorServiceClient{
		connect: connect.NewClient[v1.ConnectRequest, v1.ServerEvent](
			httpClient,
			baseURL+ConnectorServiceConnectProcedure,
			connect.WithSchema(connectorServiceMethods.ByName("Connect")),
			connect.WithClientOptions(opts...),
		),
		listConnectors: connect.NewClient[v1.ListConnectorsRequest, v1.ListConnectorsResponse](
			httpClient,
			baseURL+ConnectorServiceListConnectorsProcedure,
			connect.WithSchema(connectorServiceMethods.ByName("ListConnectors")),
			connect.WithClientOptions(opts...),
		),
	}
}

// connectorServiceClient implements ConnectorServiceClient.
type connectorServiceClient struct {
	connect        *connect.Client[v1.ConnectRequest, v1.ServerEvent]
	listConnectors *connect.Client[v1.ListConnectorsRequest, v1.ListConnectorsResponse]
}

// Connect calls connector_iface.v1.ConnectorService.Connect.
func (c *connectorServiceClient) Connect(ctx context.Context, req *connect.Request[v1.ConnectRequest]) (*connect.ServerStreamForClient[v1.ServerEvent], error) {
	return c.connect.CallServerStream(ctx, req)
}

// ListConnectors calls connector_iface.v1.ConnectorService.ListConnectors.
func (c *connectorServiceClient) ListConnectors(ctx context.Context, req *connect.Request[v1.ListConnectorsRequest]) (*connect.Response[v1.ListConnectorsResponse], error) {
	return c.listConnectors.CallUnary(ctx, req)
}

// ConnectorServiceHandler is an implementation of the connector_iface.v1.ConnectorService service.
type ConnectorServiceHandler interface {
	// Connect is the long-lived outbound stream from a connector to the server.
	// It is public + UNAUTHENTICATED by design: a connector connects freely (no
	// token), suitable for a trusted single-shop LAN. The unary auth interceptor
	// does not cover streams anyway.
	Connect(context.Context, *connect.Request[v1.ConnectRequest], *connect.ServerStream[v1.ServerEvent]) error
	// ListConnectors returns the connectors currently connected + their printers,
	// for the Settings ▸ Printing picker AND the POS receipt printer picker. Open
	// to every POS role (returns only device + printer names, nothing sensitive).
	ListConnectors(context.Context, *connect.Request[v1.ListConnectorsRequest]) (*connect.Response[v1.ListConnectorsResponse], error)
}

// NewConnectorServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewConnectorServiceHandler(svc ConnectorServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	connectorServiceMethods := v1.File_connector_iface_v1_connector_proto.Services().ByName("ConnectorService").Methods()
	connectorServiceConnectHandler := connect.NewServerStreamHandler(
		ConnectorServiceConnectProcedure,
		svc.Connect,
		connect.WithSchema(connectorServiceMethods.ByName("Connect")),
		connect.WithHandlerOptions(opts...),
	)
	connectorServiceListConnectorsHandler := connect.NewUnaryHandler(
		ConnectorServiceListConnectorsProcedure,
		svc.ListConnectors,
		connect.WithSchema(connectorServiceMethods.ByName("ListConnectors")),
		connect.WithHandlerOptions(opts...),
	)
	return "/connector_iface.v1.ConnectorService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ConnectorServiceConnectProcedure:
			connectorServiceConnectHandler.ServeHTTP(w, r)
		case ConnectorServiceListConnectorsProcedure:
			connectorServiceListConnectorsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedConnectorServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedConnectorServiceHandler struct{}

func (UnimplementedConnectorServiceHandler) Connect(context.Context, *connect.Request[v1.ConnectRequest], *connect.ServerStream[v1.ServerEvent]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("connector_iface.v1.ConnectorService.Connect is not implemented"))
}

func (UnimplementedConnectorServiceHandler) ListConnectors(context.Context, *connect.Request[v1.ListConnectorsRequest]) (*connect.Response[v1.ListConnectorsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("connector_iface.v1.ConnectorService.ListConnectors is not implemented"))
}
